# Price problem coding challenge
1. Using minitest/autorun all the tests are run from the command line
2. test_setup.rb deals with unit tests //
3. test_price.rb is the spec file that ties into unit tests //
4. Running minitests from the command happens with the 'rake test' command
5. Running specific tests from the command line simply write ruby tests/test_setup.rb -v-n test_name_of_method //


