= Price problem coding challenge
1. Using minitest/autorun ensure all the tests are run from the command line
2. Running minitests from the command happens with the 'rake test' command from price_problem directory
3. Running specific tests from the command line simply write ruby tests/test_product.rb -v-n test_name_of_test_method



